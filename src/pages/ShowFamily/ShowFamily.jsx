import Admin from "../Admin/Admin";

const Board = () => {
    
    //const table=Admin()
    Admin.arguments.
console.log();


    return <>
    
        {/* <div style={{ display: 'grid', gridTemplateColumns: `repeat(${table?.width}, 50px)` }}>
            {
                table && table.list && table.list.map((item, i) => (
                    <Square key={i}
                        value={item.value}
                        open={true}></Square>
                )
                )
            }

        </div> */}
    </>
}
export default Board